import { Element } from "react-scroll";

const Pricing = () => {
  return (
    <section>
      <Element name="pricing">
        {/* Content goes here */}
      </Element>
    </section>
  );
};

export default Pricing;
